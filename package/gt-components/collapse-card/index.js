import GCollapseCard from "./src/collapse-card.vue";

/* istanbul ignore next */
GCollapseCard.install = function (Vue) {
  Vue.component(GCollapseCard.name, GCollapseCard);
};

export default GCollapseCard;
