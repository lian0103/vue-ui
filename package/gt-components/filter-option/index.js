import GFilterOption from "./src/filter-option.vue";

/* istanbul ignore next */
GFilterOption.install = function (Vue) {
  Vue.component(GFilterOption.name, GFilterOption);
};

export default GFilterOption;
