import GInputNumber from "./src/input-number.vue";

/* istanbul ignore next */
GInputNumber.install = function (Vue) {
  Vue.component(GInputNumber.name, GInputNumber);
};

export default GInputNumber;
