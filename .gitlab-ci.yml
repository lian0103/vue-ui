image: node:16.13.2
pages:
  stage: deploy
  tags:
    - docker-build
  script:
    - echo "//prg.greattree.com.tw/repository/npm-gt/:_authToken=$npmtoken" > ~/.npmrc
    - echo "registry=$registryurl" >> ~/.npmrc
    - npm publish
  only:
    - feature
